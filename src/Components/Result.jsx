import React from 'react'

export default function Result({a,b,operator}) {
    const calculator={
        switch(operator){
            
        
        }
    }
  return (
    <div>
        {alert(a, b)}
    </div>
  )
}
